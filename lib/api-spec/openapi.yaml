openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.2.0
  description: SafeSphere API specification
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
  - name: auth
  - name: contacts
  - name: sos
  - name: location
  - name: media
  - name: incidents
  - name: responders
paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  /auth/signup:
    post:
      operationId: signup
      tags: [auth]
      summary: Create a new account and send OTP
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SignupBody"
      responses:
        "200":
          description: OTP sent
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OtpChallenge"

  /auth/login:
    post:
      operationId: login
      tags: [auth]
      summary: Login with email + password, returns OTP challenge
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginBody"
      responses:
        "200":
          description: OTP sent
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OtpChallenge"

  /auth/verify-otp:
    post:
      operationId: verifyOtp
      tags: [auth]
      summary: Verify OTP and start session
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VerifyOtpBody"
      responses:
        "200":
          description: Session established
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthSession"

  /auth/logout:
    post:
      operationId: logout
      tags: [auth]
      summary: End session
      responses:
        "200":
          description: Logged out
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SimpleOk"

  /auth/me:
    get:
      operationId: getMe
      tags: [auth]
      summary: Get current authenticated user
      responses:
        "200":
          description: Current user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"

  /contacts:
    get:
      operationId: listContacts
      tags: [contacts]
      summary: List trusted contacts
      responses:
        "200":
          description: List of contacts
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Contact"
    post:
      operationId: createContact
      tags: [contacts]
      summary: Add trusted contact
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateContactBody"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Contact"

  /contacts/{id}:
    delete:
      operationId: deleteContact
      tags: [contacts]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted

  /sos/trigger:
    post:
      operationId: triggerSos
      tags: [sos]
      summary: Trigger SOS — creates an incident, sends alerts to contacts
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TriggerSosBody"
      responses:
        "201":
          description: Incident created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Incident"

  /sos/{id}/cancel:
    post:
      operationId: cancelIncident
      tags: [sos]
      summary: Cancel an active incident (false alarm)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Incident cancelled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Incident"

  /sos/{id}/resolve:
    post:
      operationId: resolveIncident
      tags: [sos]
      summary: Mark incident as resolved (safe)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Resolved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Incident"

  /sos/{id}/escalate-nearby:
    post:
      operationId: escalateNearby
      tags: [sos]
      summary: Escalate to nearby SafeSphere users when no response
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Escalation result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EscalationResult"

  /sos/{id}/motion:
    post:
      operationId: reportMotion
      tags: [sos]
      summary: Report device motion sample for AI risk scoring
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MotionSampleBody"
      responses:
        "200":
          description: Updated risk
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RiskUpdate"

  /sos/{id}/responders:
    get:
      operationId: getIncidentResponders
      tags: [responders]
      summary: List responders engaged on this incident
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Responders
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/IncidentResponder"

  /sos/active:
    get:
      operationId: getActiveIncident
      tags: [sos]
      summary: Get currently active incident if any
      responses:
        "200":
          description: Active incident or null
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ActiveIncidentResponse"

  /responders/availability:
    post:
      operationId: setResponderAvailability
      tags: [responders]
      summary: Toggle whether you are available to help nearby users
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AvailabilityBody"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AvailabilityState"

  /responders/availability/me:
    get:
      operationId: getMyAvailability
      tags: [responders]
      summary: Get current responder availability state
      responses:
        "200":
          description: Current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AvailabilityState"

  /responders/invitations:
    get:
      operationId: listMyInvitations
      tags: [responders]
      summary: List active SOS invitations sent to me as a nearby helper
      responses:
        "200":
          description: Invitations
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/HelperInvitation"

  /responders/invitations/{id}/accept:
    post:
      operationId: acceptInvitation
      tags: [responders]
      summary: Accept an SOS invitation as a helper
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AcceptInvitationBody"
      responses:
        "200":
          description: Accepted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HelperInvitation"

  /responders/invitations/{id}/decline:
    post:
      operationId: declineInvitation
      tags: [responders]
      summary: Decline an SOS invitation as a helper
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Declined
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HelperInvitation"

  /location/update:
    post:
      operationId: updateLocation
      tags: [location]
      summary: Push a live location point for an active incident
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LocationUpdateBody"
      responses:
        "200":
          description: Stored
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SimpleOk"

  /location/incident/{id}:
    get:
      operationId: getIncidentLocations
      tags: [location]
      summary: Get all location points for an incident
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Location points
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LocationPoint"

  /media/upload:
    post:
      operationId: uploadMedia
      tags: [media]
      summary: Upload audio/video evidence (encrypted at rest)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UploadMediaBody"
      responses:
        "201":
          description: Media stored
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MediaItem"

  /incidents/history:
    get:
      operationId: getIncidentHistory
      tags: [incidents]
      summary: List user's past incidents (newest first)
      responses:
        "200":
          description: History
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Incident"

  /incidents/{id}:
    get:
      operationId: getIncidentDetail
      tags: [incidents]
      summary: Detailed incident with alerts, locations, media, responders
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IncidentDetail"

  /incidents/{id}/share:
    get:
      operationId: getIncidentShareSummary
      tags: [incidents]
      summary: Generate a shareable plain-text evidence summary
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Share payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IncidentShareSummary"

  /incidents/stats:
    get:
      operationId: getIncidentStats
      tags: [incidents]
      summary: Aggregate stats for the dashboard
      responses:
        "200":
          description: Stats
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IncidentStats"

components:
  schemas:
    HealthStatus:
      type: object
      required: [status]
      properties:
        status: { type: string }

    SimpleOk:
      type: object
      required: [ok]
      properties:
        ok: { type: boolean }

    SignupBody:
      type: object
      required: [name, email, phone, password]
      properties:
        name: { type: string }
        email: { type: string }
        phone: { type: string }
        password: { type: string }

    LoginBody:
      type: object
      required: [email, password]
      properties:
        email: { type: string }
        password: { type: string }

    OtpChallenge:
      type: object
      required: [challengeId, devOtp]
      properties:
        challengeId: { type: string }
        devOtp:
          type: ["string", "null"]
          description: Returned in dev mode so users can complete the flow without SMS gateway

    VerifyOtpBody:
      type: object
      required: [challengeId, code]
      properties:
        challengeId: { type: string }
        code: { type: string }

    AuthSession:
      type: object
      required: [token, user]
      properties:
        token: { type: string }
        user:
          $ref: "#/components/schemas/User"

    User:
      type: object
      required: [id, name, email, phone, createdAt, responderStatus]
      properties:
        id: { type: integer }
        name: { type: string }
        email: { type: string }
        phone: { type: string }
        responderStatus: { type: string }
        helperAlias:
          type: ["string", "null"]
        createdAt: { type: string, format: date-time }

    Contact:
      type: object
      required: [id, name, phone, relation, createdAt]
      properties:
        id: { type: integer }
        name: { type: string }
        phone: { type: string }
        relation: { type: string }
        createdAt: { type: string, format: date-time }

    CreateContactBody:
      type: object
      required: [name, phone, relation]
      properties:
        name: { type: string }
        phone: { type: string }
        relation: { type: string }

    TriggerSosBody:
      type: object
      required: [trigger, lat, lng]
      properties:
        trigger:
          type: string
          description: button | voice | lock-screen | shake
        lat: { type: number }
        lng: { type: number }
        accuracy:
          type: ["number", "null"]
        discreet:
          type: ["boolean", "null"]
        message:
          type: ["string", "null"]

    MotionSampleBody:
      type: object
      required: [speed]
      properties:
        speed:
          type: number
          description: m/s
        accel:
          type: ["number", "null"]
          description: m/s² peak
        repeated:
          type: ["boolean", "null"]
          description: User pressed SOS multiple times

    RiskUpdate:
      type: object
      required: [incidentId, riskScore, riskLevel, escalationSeconds]
      properties:
        incidentId: { type: integer }
        riskScore: { type: integer }
        riskLevel: { type: string }
        escalationSeconds: { type: integer }

    Incident:
      type: object
      required:
        [
          id,
          status,
          trigger,
          startLat,
          startLng,
          alertsSent,
          escalated,
          discreet,
          riskScore,
          riskLevel,
          createdAt,
        ]
      properties:
        id: { type: integer }
        status: { type: string }
        trigger: { type: string }
        startLat: { type: number }
        startLng: { type: number }
        alertsSent: { type: integer }
        escalated: { type: boolean }
        discreet: { type: boolean }
        riskScore: { type: integer }
        riskLevel: { type: string }
        motionMaxSpeed:
          type: ["number", "null"]
        triggerCount24h:
          type: ["integer", "null"]
        message:
          type: ["string", "null"]
        resolvedAt:
          type: ["string", "null"]
          format: date-time
        createdAt: { type: string, format: date-time }

    ActiveIncidentResponse:
      type: object
      required: [incident]
      properties:
        incident:
          oneOf:
            - $ref: "#/components/schemas/Incident"
            - type: "null"

    EscalationResult:
      type: object
      required: [incidentId, nearbyAlerted, contactsNotified]
      properties:
        incidentId: { type: integer }
        nearbyAlerted: { type: integer }
        contactsNotified: { type: integer }

    LocationUpdateBody:
      type: object
      required: [incidentId, lat, lng]
      properties:
        incidentId: { type: integer }
        lat: { type: number }
        lng: { type: number }
        accuracy:
          type: ["number", "null"]
        speed:
          type: ["number", "null"]

    LocationPoint:
      type: object
      required: [id, incidentId, lat, lng, encrypted, createdAt]
      properties:
        id: { type: integer }
        incidentId: { type: integer }
        lat: { type: number }
        lng: { type: number }
        accuracy:
          type: ["number", "null"]
        speed:
          type: ["number", "null"]
        encrypted: { type: boolean }
        createdAt: { type: string, format: date-time }

    UploadMediaBody:
      type: object
      required: [incidentId, kind, data]
      properties:
        incidentId: { type: integer }
        kind:
          type: string
          description: audio | video | photo
        data:
          type: string
          description: Base64-encoded media payload (encrypted at rest)
        mimeType:
          type: ["string", "null"]
        durationMs:
          type: ["integer", "null"]

    MediaItem:
      type: object
      required: [id, incidentId, kind, sizeBytes, encrypted, createdAt]
      properties:
        id: { type: integer }
        incidentId: { type: integer }
        kind: { type: string }
        url:
          type: ["string", "null"]
        sizeBytes: { type: integer }
        durationMs:
          type: ["integer", "null"]
        mimeType:
          type: ["string", "null"]
        encrypted: { type: boolean }
        createdAt: { type: string, format: date-time }

    AlertItem:
      type: object
      required:
        [id, incidentId, channel, target, status, attempts, priority, createdAt]
      properties:
        id: { type: integer }
        incidentId: { type: integer }
        channel: { type: string }
        target: { type: string }
        status: { type: string }
        attempts: { type: integer }
        priority: { type: string }
        lastError:
          type: ["string", "null"]
        deliveredAt:
          type: ["string", "null"]
          format: date-time
        createdAt: { type: string, format: date-time }

    IncidentResponder:
      type: object
      required:
        [id, incidentId, alias, status, distanceKm, createdAt]
      properties:
        id: { type: integer }
        incidentId: { type: integer }
        alias: { type: string }
        status: { type: string }
        distanceKm: { type: number }
        etaMinutes:
          type: ["integer", "null"]
        createdAt: { type: string, format: date-time }
        respondedAt:
          type: ["string", "null"]
          format: date-time

    IncidentDetail:
      type: object
      required: [incident, alerts, locations, media, responders]
      properties:
        incident:
          $ref: "#/components/schemas/Incident"
        alerts:
          type: array
          items:
            $ref: "#/components/schemas/AlertItem"
        locations:
          type: array
          items:
            $ref: "#/components/schemas/LocationPoint"
        media:
          type: array
          items:
            $ref: "#/components/schemas/MediaItem"
        responders:
          type: array
          items:
            $ref: "#/components/schemas/IncidentResponder"

    IncidentShareSummary:
      type: object
      required: [incidentId, summary, mapsUrl]
      properties:
        incidentId: { type: integer }
        summary: { type: string }
        mapsUrl: { type: string }

    AvailabilityBody:
      type: object
      required: [status]
      properties:
        status:
          type: string
          description: available | unavailable

    AvailabilityState:
      type: object
      required: [status, alias]
      properties:
        status: { type: string }
        alias: { type: string }

    HelperInvitation:
      type: object
      required:
        [
          id,
          incidentId,
          alias,
          status,
          distanceKm,
          createdAt,
          victimAlias,
          incidentLat,
          incidentLng,
          riskLevel,
        ]
      properties:
        id: { type: integer }
        incidentId: { type: integer }
        alias: { type: string }
        status: { type: string }
        distanceKm: { type: number }
        etaMinutes:
          type: ["integer", "null"]
        createdAt: { type: string, format: date-time }
        respondedAt:
          type: ["string", "null"]
          format: date-time
        victimAlias: { type: string }
        incidentLat: { type: number }
        incidentLng: { type: number }
        riskLevel: { type: string }

    AcceptInvitationBody:
      type: object
      properties:
        etaMinutes:
          type: ["integer", "null"]

    IncidentStats:
      type: object
      required:
        [
          totalIncidents,
          resolvedIncidents,
          cancelledIncidents,
          activeIncidents,
          totalAlerts,
          totalContacts,
          last7Days,
        ]
      properties:
        totalIncidents: { type: integer }
        resolvedIncidents: { type: integer }
        cancelledIncidents: { type: integer }
        activeIncidents: { type: integer }
        totalAlerts: { type: integer }
        totalContacts: { type: integer }
        last7Days:
          type: array
          items:
            type: object
            required: [day, count]
            properties:
              day: { type: string }
              count: { type: integer }
